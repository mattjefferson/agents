---
name: resolve_parallel
description: Resolve all pending CLI tasks using parallel processing
argument-hint: "[optional: specific task ID or pattern]"
---

Use the `resolve_task_parallel` skill for this command and follow its instructions.
Pass command arguments through to the skill as: `$ARGUMENTS`.
