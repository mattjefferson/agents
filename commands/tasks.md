---
description: Unified task operations with backend auto-routing
argument-hint: "<detect|create|list|show|update|claim|close|search> [arguments]"
---

Use the `tasks-core` skill for this command and follow its instructions.
Pass command arguments through to the skill as: `$ARGUMENTS`.
