---
description: Run native macOS UI smoke tests using Peekaboo
argument-hint: "[app name, bundle id, or 'frontmost']"
---

Use the `test-mac` skill for this command and follow its instructions.
Pass command arguments through to the skill as: `$ARGUMENTS`.
